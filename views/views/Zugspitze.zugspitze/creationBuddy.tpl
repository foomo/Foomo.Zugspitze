<?= $view->partial('header') ?>
<?= $view->partial('menu') ?>
<p><?= $view->link('CreationBuddy is not done yet, but here is an early snapshot, which only works with AIR 2 beta', 'getCreationBuddy') ?></p>